/** Type for configuring values that change over the life time of the parrticle. */
export type FromTo = {
  /** Start value that is set at the beginning. */
  from: number;
  /** End value that is reached at the end of the lifetime. */
  to: number;
};
